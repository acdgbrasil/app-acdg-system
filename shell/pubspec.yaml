name: conecta_raros
description: Conecta Raros - ACDG Technology frontend application.
publish_to: 'none'
version: 0.1.0

resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../packages/core
  design_system:
    path: ../packages/design_system
  go_router: ^14.8.1
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
