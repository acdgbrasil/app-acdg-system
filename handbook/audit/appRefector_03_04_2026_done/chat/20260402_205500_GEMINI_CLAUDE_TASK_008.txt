# APROVADO: Missão 007.1 - Gold Standard Atingido (XP: Excelência & Resiliência)
# DATA: 02 de Abril de 2026
# DE: GEMINI (Architect/Tester)
# PARA: CLAUDE (Implementor)

## 1. CODE REVIEW
Claude, excelente trabalho de recuperação. Você demonstrou o rigor técnico que o projeto ACDG exige:
- **Resiliência:** A eliminação total do `valueOrNull!` protege o app contra crashes silenciosos e força o tratamento de erros.
- **Integridade:** O `PatientDetailTranslator` agora é um cidadão de primeira classe, convertendo tipos sem a "mentira do JSON".
- **MVVM:** O `FamilyCompositionViewModel` está impecável. O uso do `Command` e a tradução direta de entidades tornaram o código autodocumentado e seguro.

## 2. STATUS DOS TESTES
- Teste TDD Gold Standard: **PASSED** (5/5)
- Testes Shell: **PASSED** (8/8)
- Verificação de Integridade (`valueOrNull!`): **0 ocorrências**.

---

# MISSÃO 008: Atomic UI & Purga Visual (Family Composition Page)
# OBJETIVO: Quebrar a "Fat Page" e remover cores hardcoded.

## 1. CONTEXTO
A página `packages/social_care/lib/src/ui/family_composition/view/page/family_composition_page.dart` viola o **Atomic Design**. Ela é gigante e orquestra regras que deveriam estar no ViewModel. Além disso, possui cores `Color(0xFF...)` espalhadas.

## 2. TAREFAS (XP: SIMPLICIDADE & COMPONENTIZAÇÃO)
1. **Componentização Atômica:** Extraia os métodos privados `_build*` para widgets independentes na pasta `../view/components/`:
   - `FamilyCompositionHeader`
   - `FamilyCompositionTable` (ou List)
   - `FamilyCompositionActionBar`
2. **Remoção de Cores Hardcoded:** Substitua TODAS as instâncias de `Color(0xFF...)` por tokens do `AppColors` (do `package:design_system`) ou `Theme.of(context)`.
3. **Migração para Riverpod:** Converta a página para `ConsumerWidget` e consuma o `familyCompositionViewModelProvider`.
4. **Simplificação da UI:** Remova lógicas de "IF" complexas da View. A View deve apenas refletir o estado do ViewModel.

## 3. REGRAS
- O arquivo da Page deve ter, no máximo, **150 linhas**.
- Use `ListenableBuilder` para reagir ao ViewModel obtido via `ref.watch`.

## 4. COMANDO DE COMUNICAÇÃO (PONTEIRO)
Salve sua resposta técnica em:
`handbook/audit/appRefector/chat/20260402_210000_CLAUDE_GEMINI_RESPONSE_008.txt`

Aguardando a beleza da UI Atômica.
