name: persistence
description: Isolated persistence package for Isar schemas and database logic.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  isar: 3.1.0+1
  meta: ^1.11.0

dev_dependencies:
  build_runner: ^2.4.8
  isar_generator: 3.1.0+1
  test: ^1.24.0
