name: core
description: Core package - base classes and platform resolution.
version: 0.1.2
publish_to: 'none'

resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  core_contracts:
    path: ../core_contracts
  meta: ^1.11.0
  logging: ^1.2.0
  dio: ^5.7.0
  dart_jsonwebtoken: ^3.0.0
  persistence:
    path: ../persistence
  drift: ^2.22.1
  drift_flutter: ^0.2.4
  path_provider: ^2.1.5
  sentry_flutter: ^8.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
