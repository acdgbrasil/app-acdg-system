name: social_care
description: Social Care module — domain, data, and UI layers for patient management.
version: 0.1.0
publish_to: 'none'

resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  shared:
    path: ../../bff/shared

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
