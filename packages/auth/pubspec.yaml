name: auth
description: Authentication package - OIDC auth, roles, tokens, and user models.
version: 0.1.1
publish_to: 'none'

resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  http: ^1.4.0
  oidc: ^0.14.0+2
  oidc_default_store: ^0.6.0+2
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
