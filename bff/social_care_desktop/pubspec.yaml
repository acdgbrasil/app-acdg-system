name: social_care_desktop
description: A starting point for Dart libraries or applications.
version: 1.2.0
publish_to: 'none'

resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  shared:
    path: ../shared
  core_contracts:
    path: ../../packages/core_contracts
  network:
    path: ../../packages/network
  persistence:
    path: ../../packages/persistence
  core:
    path: ../../packages/core
  flutter:
    sdk: flutter
  dio: ^5.9.2
  drift: ^2.31.0
  sqlite3_flutter_libs: ^0.5.28
  path_provider: ^2.1.0
  connectivity_plus: ^7.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  drift_dev: ^2.31.0
  build_runner: ^2.4.0
  sqlite3: ^2.7.4
