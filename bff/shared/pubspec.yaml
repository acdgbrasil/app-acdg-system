name: shared
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: 'none'

resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  core:
    path: ../../packages/core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
